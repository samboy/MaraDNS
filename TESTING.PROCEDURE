The testing is done with a shell script.

Required: Git, valgrind, gcc, make, etc.

Testing platfor: CentOS 7

To test:

```
cp tools/OneSourceOfTruth/do.osot.tests ~/tmp
cd ~/tmp
su
./do.osot.tests
```
