--- maradns-1.3.04/doc/en/source/changelog.embed	2007-03-22 01:31:27.000000000 -0600
+++ maradns-1.3.05/doc/en/source/changelog.embed	2007-05-21 06:26:19.000000000 -0500
@@ -1,5 +1,37 @@
 <H1>MaraDNS changelog</H1>
 
+<p>maradns-1.3.05:</p>
+This is a testing release of MaraDNS.
+<blockquote>
+<ul>
+<li>New mararc variable, <tt>max_mem</tt> added, that limits the amount
+    of memory MaraDNS will allocate.
+<li>Bugfix: Whether to give a NXDOMAIN or a "not there" reply with star
+    records fixed to be RFC1034 and RFC4074 compliant.
+<li>Bugfix: Reply for hosts not in a <tt>csv2_default_zonefile</tt> now 
+    correctly return a "not there" instead of a NXDOMAIN (unless there is no
+    host of any RR type that matches the desired name).
+<li>Bugfix: Jo&atilde;o Antunes Predator tool found two memory leaks.  
+    Fixed.
+</ul>
+(2007.05.21)
+</blockquote>
+
+<p>maradns-1.2.12.06:</p>
+This is a stable release of MaraDNS.
+<blockquote>
+<ul>
+<li>Bugfix: Whether to give a NXDOMAIN or a "not there" reply with star
+    records fixed to be RFC1034 and RFC4074 compliant.
+<li>Bugfix: Reply for hosts not in a <tt>csv2_default_zonefile</tt> now 
+    correctly return a "not there" instead of a NXDOMAIN (unless there is no
+    host of any RR type that matches the desired name).
+<li>Bugfix: Jo&atilde;o Antunes Predator tool found two memory leaks.  
+    Fixed.
+</ul>
+(2007.05.17)
+</blockquote>  
+
 <p>maradns-1.3.04:<p>
 This is a testing release of MaraDNS.
 <blockquote>
