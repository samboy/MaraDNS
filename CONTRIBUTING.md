The only changes I am making are:

* Security fixes
* Patches for any behavior which raises a warning or error in Valgrind

This party patches are not accepted.  Third party security bug reports
are accepted, but I will verify security reports before considering
them a security bug in MaraDNS.

Valgrind warnings/errors will be patched, but only if one gives a working
recipe to reproduce the warning/error.
